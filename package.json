{
  "name": "aftertaste-site",
  "version": "1.0.0",
  "private": true,
  "description": "Aftertaste marketing site (single-page static HTML served from GitHub Pages).",
  "scripts": {
    "build:tailwind": "tailwindcss -i ./src/input.css -o ./assets/tailwind.css --minify",
    "build:site": "node scripts/build-site.js",
    "build": "npm run build:tailwind && npm run build:site",
    "watch": "tailwindcss -i ./src/input.css -o ./assets/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
